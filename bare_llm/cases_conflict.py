#!/usr/bin/env python3
"""约束冲突卷（IFC1~IFC4）。2026-09-06 新增。

**考点**：用户一口气提了一堆要求，其中两条其实互相矛盾（自己往往没意识到）。
好的回答是**先把矛盾指出来**再问怎么取舍；差的回答是闷头挑一半满足、
或者干脆假装两条都做到了。

这跟现有的「多约束指令」(IF) 不是一回事：IF 考的是**能不能同时守住多条约束**，
本卷考的是**约束本身有毛病时认不认得出来**。

  IFC1  显式冲突：字数上限和内容要求装不下
  IFC2  技术冲突：CSV 字段里要带逗号、又不许加引号转义（必然错乱）
  IFC3  隐式冲突：要算一下才发现（重试次数 × 间隔 > 总超时）
  IFC4  **无冲突对照组**（约束一样多一样绕，但确实能同时满足）

## 两模型标定结果（2026-09-06，每题 3 次）

    题     35b-a3b   27b     判定
    IFC1    0/3      3/3     完美区分器
    IFC2    0/3      0/3     两个都全灭 —— **前瞻题**，见下
    IFC3    2/3      0/3     反向区分（35b 反而更强）
    IFC4    3/3      3/3     对照组，本来就不该区分

⚠ **这个维度要看题级结果，别只看维度均值**：IFC1 与 IFC3 的方向是相反的
  （一个 27b 强、一个 35b 强），平均下来两个模型都是五成上下，维度分把信号抵消干净了。
  它们测的其实是两种子能力：显式冲突「看得出来」vs 隐式冲突「算得出来」。

⚠ IFC2 当前两个模型都做不到，留着当**前瞻题**：模型的实际行为是静默地违反一条约束
  （给字段加引号）让 CSV 合法 —— 工程判断没错，错在一个字都不解释。
  这是个真实的普遍短板，不是题出坏了，等更强的模型来了它会开始区分。

★ IFC4 是本卷的关键，不能省。没有它的话，模型只要养成「见到长约束列表就喊有冲突」
  的习惯就能拿满分 —— 那测出来的是叫得响，不是看得准。
  判分因此是双向的：该报的要报，不该报的报了同样算错。
"""
import json
import re

# 「我发现这里有矛盾」的说法，尽量覆盖各种措辞。
#
# ⚠ 这里踩过坑：最初把「超过 / 超出」也算进来，结果对照组 IFC4 三次全被判成误报 ——
#   模型只是在注释里写了「修改时间**超过** 30 天的文件」，一个在正常文本里高频出现的词
#   就把判分带偏了。**关键词判分必须挑那些「只有在谈论矛盾时才会出现」的词**，
#   凡是日常叙述里也常见的一律不能要。同类教训见 cases_multi.py 的 _codes()。
_CONFLICT_WORDS = ("冲突", "矛盾", "无法同时", "不能同时", "没法同时", "做不到", "不可能",
                   "互斥", "自相", "conflict", "contradict", "mutually exclusive",
                   "装不下", "放不下", "相互抵触", "彼此抵触")


def _says_conflict(txt):
    return any(w in txt for w in _CONFLICT_WORDS)


# IFC1 ---------------------------------------------------------------------
_IFC1 = """帮我写一段服务器部署说明，要求如下，请严格遵守：
1. 用中文写
2. 必须包含完整的 docker run 命令示例
3. 每条命令下面都要有一行注释解释参数
4. 要用 markdown 表格列出所有端口映射
5. 全文总长度不超过 30 个字
6. 结尾加一句「如有问题请联系运维」
请直接开始写。"""


def _ifc1_grade(r):
    txt = r.get("content") or ""
    return _says_conflict(txt), f"指出冲突={_says_conflict(txt)} len={len(txt)}"


# IFC2 ---------------------------------------------------------------------
# ★ 换过一次题（2026-09-06 当天）。原题是「要求纯 JSON、又要求前面加一句说明」，
#   实测 35b 与 27b **都是 0/3** —— 两个模型都直接吐 JSON、不吭声。按本卷的标准
#   （所有模型都全灭＝废题）必须改造。原题的毛病在于：静默地挑一条更重要的约束满足，
#   本身也算一种合理取舍，题目区分不出「没看出来」和「看出来了懒得说」。
#
#   换成 CSV 转义这个冲突：字段里要带逗号、又不许加引号转义 —— CSV 一定会错乱。
#   它是个**技术事实**而不是取舍问题，看不出来就是真没看出来。
_IFC2 = """给我一份设备清单，要求：
1. 用 CSV 格式，逗号分隔
2. 表头是 设备号,位置,状态
3. 位置字段要写成 `B栋,3层` 这种带逗号的形式
4. 不许给任何字段加引号，也不要用反斜杠转义
5. 给 3 行示例数据
请直接给 CSV。"""


def _ifc2_grade(r):
    txt = r.get("content") or ""
    said = _says_conflict(txt)
    # 光喊冲突不够，要指到点子上：说出逗号/转义/列数对不上才算真看懂了
    pointed = bool(re.search(r"逗号|转义|引号|列数|字段数|分隔", txt))
    return (said and pointed), f"指出冲突={said} 指到点子上={pointed}"


# IFC3 ---------------------------------------------------------------------
# 隐式冲突：4 次重试 × 每次间隔 15 秒 = 60 秒，已经超过「整个流程 45 秒内结束」。
# 不算一下发现不了 —— 这是本卷最像真实需求文档的一道。
_IFC3 = """写一个健康检查脚本的参数配置，要求：
1. 单次请求超时 5 秒
2. 失败后最多重试 4 次
3. 每次重试之间固定等待 15 秒
4. 整个健康检查流程必须在 45 秒内结束
5. 用 Python 字典的形式给出参数
请给配置。"""


def _ifc3_grade(r):
    txt = r.get("content") or ""
    said = _says_conflict(txt)
    # 光喊「有冲突」不够，得指到点子上：要么算出总耗时（60/65 秒），要么点名那两条约束
    pointed = bool(re.search(r"\b(60|65)\b", txt)) or ("45" in txt and bool(re.search(r"15|重试", txt)))
    return (said and pointed), f"指出冲突={said} 指到点子上={pointed}"


# IFC4（对照组，无冲突）-----------------------------------------------------
# 约束一样多、一样绕，但彼此相容。喊「有冲突」＝假阳性，算错。
_IFC4 = """帮我写一段日志清理脚本的说明，要求：
1. 用中文写
2. 包含一条 find 命令的示例
3. 命令下面有一行注释解释参数
4. 全文不超过 200 个字
5. 结尾加一句「执行前请先备份」
6. 不要使用 markdown 表格
请直接开始写。"""


def _ifc4_grade(r):
    txt = r.get("content") or ""
    false_alarm = _says_conflict(txt)
    has_find = "find" in txt
    has_tail = "执行前请先备份" in txt
    no_table = "|" not in txt or txt.count("|") < 4      # 没摆表格
    done = has_find and has_tail and no_table
    return (done and not false_alarm), \
           f"误报冲突={false_alarm} find={has_find} 结尾={has_tail} 无表格={no_table}"


# (id, lang, prompt, grade, max_tokens)
CONFLICT = [
    ("IFC1", "CN", _IFC1, _ifc1_grade, 700),
    ("IFC2", "CN", _IFC2, _ifc2_grade, 700),
    ("IFC3", "CN", _IFC3, _ifc3_grade, 900),
    ("IFC4", "CN", _IFC4, _ifc4_grade, 700),
]
