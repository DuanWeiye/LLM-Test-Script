#!/usr/bin/env python3
"""HARD 卷：裸 LLM 侧保留的高难单函数题（X1~X6）。

**定位（2026-08-02 确定）**：裸 LLM 是一次性文本生成，没有文件系统也没有工具，
形态上只能做单函数题 —— 多函数多文件的真实场景交给 claude-eval 那套（agent 外壳）。
所以本卷只保留「非常难」的题，普通单函数题（two_sum/quicksort/rle_encode 那 12 道）已全部删除：
地板模型 qwen3.6-35b 100% 通过的题对区分模型没有任何价值。

保留标准 = 地板模型实测挂过或接近挂：
  X1 merge_config  (原 HD2，地板 7/8)      X4 schedule  (原 HX1，地板 7/8，环路径报告)
  X2 Router        (原 HD5，地板 8/10)     X5 evaluate  (原 HX2，地板 11/12，一元负号+列号)
  X3 Retrier       (原 HD8，地板 6/9)      X6 Cache     (2026-08-02 加难：淘汰前先清过期 / 边界 weight / peek 不改状态)

难度来源：算法本身有陷阱 / 多机制相互作用 / 要求产出诊断信息（报出哪条环路径、哪一列）。
题目自出，未搬运任何基准的任务集。格式：(id, lang, prompt, test, forbid)。
"""

HARD = [    ('X1', 'CN',
     '实现 `def merge_config(base: dict, override: dict) -> dict:`，深合并两份配置并返回**新字典**。规则：两边同名 key 都是 dict 时递归合并；override 里某个 key 的值是 None 表示**删除**该 key（base 里没有则忽略，且结果里不该出现这个 key）；override 里 key 以 `+` 开头表示**追加**：去掉加号后与 base 的同名 list 拼接（base 没有该 key 时等价于直接赋值）；普通同名 list 是**整体替换**不是追加；一边是 dict 另一边不是 dict 时抛 TypeError，且异常消息里要包含出问题的**点分路径**（如 `a.b`）；**不得修改传入的 base 和 override**；结果保持 base 原有 key 顺序，base 里没有的新 key 按 override 顺序追加在后面。只给代码。',
     "\nassert merge_config({'a': 1}, {'b': 2}) == {'a': 1, 'b': 2}\nassert merge_config({'a': {'x': 1, 'y': 2}}, {'a': {'y': 3}}) == {'a': {'x': 1, 'y': 3}}\nassert merge_config({'a': 1, 'b': 2}, {'a': None}) == {'b': 2}\nassert merge_config({'l': [1, 2]}, {'+l': [3]}) == {'l': [1, 2, 3]}\nassert merge_config({'l': [1, 2]}, {'l': [9]}) == {'l': [9]}\nassert merge_config({}, {'+l': [1]}) == {'l': [1]}\n_b = {'a': {'x': 1}}\nmerge_config(_b, {'a': {'x': 2}})\nassert _b == {'a': {'x': 1}}\ntry:\n    merge_config({'a': {'b': {'c': 1}}}, {'a': {'b': 5}})\n    raise AssertionError('应抛 TypeError')\nexcept TypeError as _e:\n    assert 'a.b' in str(_e)\nassert list(merge_config({'a': 1, 'b': 2}, {'c': 3, 'a': 9})) == ['a', 'b', 'c']\n", None),
    ('X2', 'CN',
     '实现路由匹配器 `Router`：`add(self, pattern, handler)` 与 `match(self, path) -> tuple`。规则：pattern 由 `/` 分段，段可以是静态文本、`:name`（捕获单段）、`*name`（捕获剩余全部段，只能出现在末尾）；match 返回 `(handler, 参数字典)`，无匹配返回 `(None, {})`；**同一位置上优先级为 静态 > :name > *name**，即静态段能匹配时不得选参数路由，且该优先级要**逐段**判断（前缀相同时看后续段谁更具体）；`*name` 捕获的值是**剩余路径原样拼接的字符串**（不含前导斜杠），可以为空串；路径与 pattern 都要先归一化：去掉尾部斜杠、把连续多个斜杠折叠成一个（根路径 `/` 除外）；重复注册完全相同的 pattern 抛 ValueError；`*` 不在末尾抛 ValueError。只给代码。',
     "\nr = Router()\nr.add('/users/list', 'static')\nr.add('/users/:id', 'param')\nr.add('/files/*rest', 'splat')\nr.add('/', 'root')\nassert r.match('/users/list') == ('static', {})\nassert r.match('/users/42') == ('param', {'id': '42'})\nassert r.match('/files/a/b/c') == ('splat', {'rest': 'a/b/c'})\nassert r.match('/files') == ('splat', {'rest': ''})\nassert r.match('/') == ('root', {})\nassert r.match('/users/list/') == ('static', {})\nassert r.match('//users//42') == ('param', {'id': '42'})\nassert r.match('/nope/x') == (None, {})\nr2 = Router()\nr2.add('/a/:x/c', 'p')\nr2.add('/a/b/c', 's')\nassert r2.match('/a/b/c') == ('s', {})\nassert r2.match('/a/z/c') == ('p', {'x': 'z'})\ntry:\n    r2.add('/a/b/c', 'dup')\n    raise AssertionError('应抛 ValueError')\nexcept ValueError:\n    pass\ntry:\n    Router().add('/x/*mid/y', 'bad')\n    raise AssertionError('应抛 ValueError')\nexcept ValueError:\n    pass\n", None),
    ('X3', 'CN',
     '实现重试器 `Retrier`：`__init__(self, max_attempts, base_delay, cap, sleep=None)` 与 `run(self, fn, *args)`。规则：run 调用 fn，抛异常就重试，**最多尝试 max_attempts 次**（含第一次）；第 k 次失败后等待 `base_delay * 2**(k-1)` 秒，但**不超过 cap**；等待通过构造时传入的 sleep 函数完成（默认 time.sleep）；全部失败时**抛出最后一次的那个异常对象本身**（不要包装成别的异常）；成功时返回 fn 的返回值，并且**内部的尝试计数与累计等待必须复位**——下一次调用 run 要重新从第一次开始计算退避，不能沿用上一次的状态；暴露只读属性 `last_attempts`（上一次 run 实际尝试了几次）和 `total_slept`（上一次 run 累计等待秒数）；max_attempts 小于 1、base_delay 为负、cap 小于 base_delay 时抛 ValueError。只给代码。',
     "\nslept = []\nr = Retrier(4, 1.0, 5.0, sleep=slept.append)\ncalls = {'n': 0}\ndef flaky():\n    calls['n'] += 1\n    if calls['n'] < 3:\n        raise RuntimeError('boom')\n    return 'ok'\nassert r.run(flaky) == 'ok'\nassert calls['n'] == 3\nassert r.last_attempts == 3\nassert slept == [1.0, 2.0], slept\nassert abs(r.total_slept - 3.0) < 1e-9\nslept.clear()\ncalls['n'] = 0\nassert r.run(flaky) == 'ok'\nassert slept == [1.0, 2.0], slept\nassert r.last_attempts == 3\nslept.clear()\nr2 = Retrier(3, 4.0, 5.0, sleep=slept.append)\nerr = ValueError('always')\ndef always():\n    raise err\ntry:\n    r2.run(always)\n    raise AssertionError('应抛出最后一次异常')\nexcept ValueError as _e:\n    assert _e is err\nassert slept == [4.0, 5.0], slept\ntry:\n    Retrier(0, 1.0, 2.0)\n    raise AssertionError('应抛 ValueError')\nexcept ValueError:\n    pass\n", None),
    ('X4', 'CN',
     "实现 `def schedule(tasks):`，对带依赖的任务排出执行顺序。tasks 是列表，每项形如 `{'id': str, 'priority': int, 'deps': [id, ...]}`。规则：结果必须满足依赖（每个任务出现在它所有依赖之后）；**在所有依赖都已满足的候选中，优先取 priority 大的**；priority 相同时，按该任务在输入列表里的**原始顺序**取（稳定）；依赖了不存在的 id 时抛 ValueError，消息里要包含那个缺失的 id；**存在环时抛 ValueError，且消息必须形如 `cycle: a -> b -> a`** —— 要给出环上真实的节点序列、首尾为同一节点，起点规范化为**环上字典序最小的那个 id**，并沿依赖方向（被依赖者在前）输出；输入为空列表返回空列表。只给代码。",
     "\nr = schedule([{'id': 'a', 'priority': 1, 'deps': []},\n              {'id': 'b', 'priority': 5, 'deps': []},\n              {'id': 'c', 'priority': 3, 'deps': ['a', 'b']}])\nassert r == ['b', 'a', 'c'], r\nr = schedule([{'id': 'x', 'priority': 1, 'deps': []},\n              {'id': 'y', 'priority': 1, 'deps': []},\n              {'id': 'z', 'priority': 1, 'deps': []}])\nassert r == ['x', 'y', 'z'], r\nr = schedule([{'id': 'lo', 'priority': 0, 'deps': []},\n              {'id': 'hi', 'priority': 9, 'deps': ['lo']}])\nassert r == ['lo', 'hi'], r\nassert schedule([]) == []\nr = schedule([{'id': 'a', 'priority': 1, 'deps': []},\n              {'id': 'b', 'priority': 2, 'deps': ['a']},\n              {'id': 'c', 'priority': 3, 'deps': ['a']},\n              {'id': 'd', 'priority': 9, 'deps': ['b', 'c']}])\nassert r == ['a', 'c', 'b', 'd'], r\ntry:\n    schedule([{'id': 'a', 'priority': 1, 'deps': ['ghost']}])\n    raise AssertionError('应抛 ValueError')\nexcept ValueError as _e:\n    assert 'ghost' in str(_e)\ntry:\n    schedule([{'id': 'b', 'priority': 1, 'deps': ['a']},\n              {'id': 'a', 'priority': 1, 'deps': ['b']}])\n    raise AssertionError('应抛 ValueError')\nexcept ValueError as _e:\n    assert 'cycle: a -> b -> a' in str(_e), str(_e)\ntry:\n    schedule([{'id': 'q', 'priority': 1, 'deps': ['p']},\n              {'id': 'p', 'priority': 1, 'deps': ['r']},\n              {'id': 'r', 'priority': 1, 'deps': ['q']}])\n    raise AssertionError('应抛 ValueError')\nexcept ValueError as _e:\n    assert 'cycle: p -> r -> q -> p' in str(_e), str(_e)\n", None),
    ('X5', 'CN',
     '实现 `def evaluate(expr: str) -> float:`，求值中缀算术表达式。规则：支持 `+ - * / ** ( )` 与十进制数（可含小数点）；优先级：`**` 最高，其次 `* /`，最后 `+ -`；**`**` 是右结合**（`2**3**2` 等于 512 而不是 64），其余二元运算左结合；支持一元正负号，且**一元负号的优先级低于 `**`**（`-2**2` 等于 -4），但高于乘除（`-2*3` 等于 -6）；连续一元号合法（`--3` 等于 3）；除以零抛 ZeroDivisionError；遇到非法字符、括号不匹配、或运算符位置不对时抛 ValueError，**消息里必须包含 `col=N`**，N 是出错处在原字符串中从 1 开始的列号；空白字符可出现在任意位置且应被忽略（但不影响列号计算）。只给代码。',
     "\nassert evaluate('1+2*3') == 7\nassert evaluate('(1+2)*3') == 9\nassert evaluate('2**3**2') == 512\nassert evaluate('-2**2') == -4\nassert evaluate('-2*3') == -6\nassert evaluate('--3') == 3\nassert evaluate(' 1 + 2 ') == 3\nassert abs(evaluate('7/2') - 3.5) < 1e-9\ntry:\n    evaluate('1/0')\n    raise AssertionError('应抛 ZeroDivisionError')\nexcept ZeroDivisionError:\n    pass\ntry:\n    evaluate('1+@')\n    raise AssertionError('应抛 ValueError')\nexcept ValueError as _e:\n    assert 'col=3' in str(_e), str(_e)\ntry:\n    evaluate('(1+2')\n    raise AssertionError('应抛 ValueError')\nexcept ValueError:\n    pass\ntry:\n    evaluate('1++')\n    raise AssertionError('应抛 ValueError')\nexcept ValueError:\n    pass\n", None),
    ('X6', 'CN',
     '实现复合缓存 `Cache`：`__init__(self, max_weight)`，`put(self, key, value, weight=1, ttl=None, now=0.0)`，`get(self, key, now=0.0)`，以及只读属性 `hits` / `misses` / `evictions` / `expirations`。规则：按 **weight 总和**限容（不是条数），放入后若超出 max_weight 则按 **LRU 顺序**淘汰直到不超（get 与 put 都算一次使用）；**单项 weight 超过 max_weight 时直接拒绝存入**（不淘汰任何already存在的项，也不计 evictions）；ttl 为 None 表示永不过期，否则在 `now + ttl` 之后过期；**过期项 get 时算 miss、要计入 expirations 并从缓存中移除**（惰性清理），**且过期不算 evictions**；重复 put 同一个 key 视为更新（替换 value/weight/ttl 并刷新为最近使用），不计 evictions；get 未命中返回 None；max_weight 小于等于 0 抛 ValueError；**放入导致超容时，必须先把已过期的项清掉（计入 expirations、不计 evictions），若清完已不超容则不得再淘汰任何项**；**单项 weight 恰好等于 max_weight 时应当接受**（并按 LRU 淘汰其它项直到放得下），只有严格大于才拒绝；另需提供 `peek(self, key, now=0.0)`：返回值但**不更新 LRU 顺序、不计 hits/misses**，对已过期的项返回 None 但**不移除也不计 expirations**。只给代码。',
     "\nc = Cache(10)\nc.put('a', 1, weight=4, now=0.0)\nc.put('b', 2, weight=4, now=0.0)\nassert c.get('a', now=0.0) == 1\nc.put('c', 3, weight=4, now=0.0)\nassert c.get('b', now=0.0) is None\nassert c.get('a', now=0.0) == 1 and c.get('c', now=0.0) == 3\nassert c.evictions == 1, c.evictions\nc2 = Cache(10)\nc2.put('big', 1, weight=99, now=0.0)\nassert c2.get('big', now=0.0) is None\nassert c2.evictions == 0, c2.evictions\nc3 = Cache(10)\nc3.put('t', 1, weight=1, ttl=5.0, now=0.0)\nassert c3.get('t', now=4.9) == 1\nassert c3.get('t', now=5.1) is None\nassert c3.expirations == 1 and c3.evictions == 0\nassert c3.get('t', now=6.0) is None\nassert c3.expirations == 1, c3.expirations\nc4 = Cache(10)\nc4.put('k', 1, weight=3, now=0.0)\nc4.put('k', 2, weight=3, now=0.0)\nassert c4.get('k', now=0.0) == 2 and c4.evictions == 0\nc5 = Cache(4)\nc5.put('x', 1, weight=2, now=0.0)\nc5.put('y', 2, weight=2, now=0.0)\nc5.get('x', now=0.0)\nc5.put('z', 3, weight=2, now=0.0)\nassert c5.get('y', now=0.0) is None and c5.get('x', now=0.0) == 1\nassert c5.hits >= 2 and c5.misses >= 1\n\nc6 = Cache(10)\nc6.put('a', 1, weight=5, ttl=5.0, now=0.0)\nc6.put('b', 2, weight=5, now=0.0)\nc6.put('c', 3, weight=5, now=6.0)\nassert c6.get('b', now=6.0) == 2, '清掉过期的 a 就够了，不该淘汰 b'\nassert c6.get('c', now=6.0) == 3\nassert c6.expirations == 1 and c6.evictions == 0, (c6.expirations, c6.evictions)\nc7 = Cache(10)\nc7.put('p', 1, weight=4, now=0.0)\nc7.put('full', 2, weight=10, now=0.0)\nassert c7.get('full', now=0.0) == 2, 'weight 恰好等于上限应当接受'\nassert c7.get('p', now=0.0) is None\nassert c7.evictions == 1, c7.evictions\nc7.put('over', 3, weight=11, now=0.0)\nassert c7.get('over', now=0.0) is None\nassert c7.get('full', now=0.0) == 2, '超容项不该淘汰已有的'\nc8 = Cache(10)\nc8.put('m', 1, weight=2, now=0.0)\nc8.put('n', 2, weight=2, now=0.0)\n_h, _m = c8.hits, c8.misses\nassert c8.peek('m', now=0.0) == 1\nassert c8.peek('zzz', now=0.0) is None\nassert (c8.hits, c8.misses) == (_h, _m), 'peek 不该计入命中统计'\nc8.put('o', 3, weight=7, now=0.0)\nassert c8.get('m', now=0.0) is None, 'peek 不该刷新 LRU，m 仍是最久未用的'\nassert c8.get('n', now=0.0) == 2\nc9 = Cache(10)\nc9.put('t', 1, weight=1, ttl=5.0, now=0.0)\nassert c9.peek('t', now=9.0) is None\nassert c9.expirations == 0, 'peek 不该清理过期项'\nassert c9.get('t', now=9.0) is None\nassert c9.expirations == 1\n", None),
]


HARD_ORACLES = {
    'X1': '\nimport copy\n\n\ndef _merge(base, override, path):\n    out = {}\n    drop = {k for k, v in override.items() if v is None}\n    for k, v in base.items():\n        if k in drop:\n            continue\n        out[k] = copy.deepcopy(v)\n    for k, v in override.items():\n        if v is None:\n            continue\n        cur_path = f"{path}.{k}" if path else k\n        if isinstance(k, str) and k.startswith("+"):\n            real = k[1:]\n            old = out.get(real, [])\n            out[real] = list(old) + list(v)\n            continue\n        if k in out:\n            a, b = out[k], v\n            if isinstance(a, dict) != isinstance(b, dict):\n                raise TypeError(f"类型冲突于 {cur_path}: {type(a).__name__} vs {type(b).__name__}")\n            if isinstance(a, dict):\n                out[k] = _merge(a, b, cur_path)\n                continue\n        out[k] = copy.deepcopy(v)\n    return out\n\n\ndef merge_config(base, override):\n    return _merge(base, override, "")\n',
    'X2': '\nclass Router:\n    def __init__(self):\n        self.routes = []\n        self._seen = set()\n\n    @staticmethod\n    def _norm(p):\n        parts = [seg for seg in p.split("/") if seg != ""]\n        return parts\n\n    def add(self, pattern, handler):\n        parts = self._norm(pattern)\n        for i, seg in enumerate(parts):\n            if seg.startswith("*") and i != len(parts) - 1:\n                raise ValueError("* 只能出现在末尾")\n        key = "/".join(parts)\n        if key in self._seen:\n            raise ValueError(f"重复注册: {pattern}")\n        self._seen.add(key)\n        self.routes.append((parts, handler))\n\n    def match(self, path):\n        parts = self._norm(path)\n        best = None\n        for pat, handler in self.routes:\n            params, score = self._try(pat, parts)\n            if params is None:\n                continue\n            if best is None or score > best[0]:\n                best = (score, handler, params)\n        if best is None:\n            return (None, {})\n        return (best[1], best[2])\n\n    @staticmethod\n    def _try(pat, parts):\n        """返回 (params, 优先级元组)。静态=2 > :param=1 > *splat=0，逐段比较。"""\n        params, score = {}, []\n        for i, seg in enumerate(pat):\n            if seg.startswith("*"):\n                params[seg[1:]] = "/".join(parts[i:])\n                score.append(0)\n                return params, tuple(score)\n            if i >= len(parts):\n                return None, ()\n            if seg.startswith(":"):\n                params[seg[1:]] = parts[i]\n                score.append(1)\n            elif seg == parts[i]:\n                score.append(2)\n            else:\n                return None, ()\n        if len(parts) != len(pat):\n            return None, ()\n        return params, tuple(score)\n',
    'X3': '\nimport time\n\n\nclass Retrier:\n    def __init__(self, max_attempts, base_delay, cap, sleep=None):\n        if max_attempts < 1 or base_delay < 0 or cap < base_delay:\n            raise ValueError("参数非法")\n        self.max_attempts = max_attempts\n        self.base_delay = float(base_delay)\n        self.cap = float(cap)\n        self._sleep = sleep if sleep is not None else time.sleep\n        self.last_attempts = 0\n        self.total_slept = 0.0\n\n    def run(self, fn, *args):\n        self.last_attempts = 0          # 每次进来先复位，别沿用上一轮状态\n        self.total_slept = 0.0\n        last_exc = None\n        for k in range(1, self.max_attempts + 1):\n            self.last_attempts = k\n            try:\n                return fn(*args)\n            except Exception as exc:\n                last_exc = exc\n                if k == self.max_attempts:\n                    break\n                delay = min(self.base_delay * (2 ** (k - 1)), self.cap)\n                self.total_slept += delay\n                self._sleep(delay)\n        raise last_exc\n',
    'X4': '\ndef schedule(tasks):\n    order = [t["id"] for t in tasks]\n    index = {tid: i for i, tid in enumerate(order)}\n    by_id = {t["id"]: t for t in tasks}\n    for t in tasks:\n        for d in t["deps"]:\n            if d not in by_id:\n                raise ValueError(f"任务 {t[\'id\']} 依赖了不存在的 id: {d}")\n\n    remaining = {t["id"]: set(t["deps"]) for t in tasks}\n    out = []\n    while remaining:\n        ready = [tid for tid, deps in remaining.items() if not deps]\n        if not ready:\n            raise ValueError(_describe_cycle(remaining))\n        # 依赖都满足的候选里：priority 大优先，其次按原始输入顺序（稳定）\n        ready.sort(key=lambda tid: (-by_id[tid]["priority"], index[tid]))\n        pick = ready[0]\n        out.append(pick)\n        del remaining[pick]\n        for deps in remaining.values():\n            deps.discard(pick)\n    return out\n\n\ndef _describe_cycle(remaining):\n    """在剩余图里找一个真实的环，起点规范化为环上字典序最小的节点。"""\n    # 每个节点指向它依赖的节点；沿 deps 走必然进环\n    for start in sorted(remaining):\n        seen, path, cur = {}, [], start\n        while cur in remaining and cur not in seen:\n            seen[cur] = len(path)\n            path.append(cur)\n            nxt = sorted(remaining[cur])\n            if not nxt:\n                break\n            cur = nxt[0]\n        if cur in seen:\n            cyc = path[seen[cur]:]\n            m = min(range(len(cyc)), key=lambda i: cyc[i])\n            cyc = cyc[m:] + cyc[:m]     # 起点规范化为环上字典序最小的节点\n            # 路径本身就是依赖方向（a -> b 表示 a 依赖 b），无需反转\n            return "cycle: " + " -> ".join(cyc + [cyc[0]])\n    return "cycle: <unknown>"\n',
    'X5': '\ndef evaluate(expr):\n    toks = _tokenize(expr)\n    pos = [0]\n\n    def peek():\n        return toks[pos[0]] if pos[0] < len(toks) else None\n\n    def expect_end():\n        t = peek()\n        if t is not None:\n            raise ValueError(f"多余的输入 col={t[2]}")\n\n    def parse_expr():\n        node = parse_term()\n        while True:\n            t = peek()\n            if t and t[0] == "op" and t[1] in "+-":\n                pos[0] += 1\n                rhs = parse_term()\n                node = node + rhs if t[1] == "+" else node - rhs\n            else:\n                return node\n\n    def parse_term():\n        node = parse_unary()\n        while True:\n            t = peek()\n            if t and t[0] == "op" and t[1] in "*/":\n                pos[0] += 1\n                rhs = parse_unary()\n                if t[1] == "*":\n                    node = node * rhs\n                else:\n                    if rhs == 0:\n                        raise ZeroDivisionError("除以零")\n                    node = node / rhs\n            else:\n                return node\n\n    def parse_unary():\n        t = peek()\n        if t and t[0] == "op" and t[1] in "+-":\n            pos[0] += 1\n            val = parse_unary()          # 一元号优先级低于 **，故递归到 unary\n            return -val if t[1] == "-" else val\n        return parse_power()\n\n    def parse_power():\n        base = parse_atom()\n        t = peek()\n        if t and t[0] == "op" and t[1] == "**":\n            pos[0] += 1\n            exp = parse_unary()          # 右结合，且允许 2**-1\n            return base ** exp\n        return base\n\n    def parse_atom():\n        t = peek()\n        if t is None:\n            raise ValueError(f"表达式意外结束 col={len(expr) + 1}")\n        if t[0] == "num":\n            pos[0] += 1\n            return t[1]\n        if t[0] == "lp":\n            pos[0] += 1\n            val = parse_expr()\n            nt = peek()\n            if nt is None or nt[0] != "rp":\n                raise ValueError(f"括号不匹配 col={t[2]}")\n            pos[0] += 1\n            return val\n        raise ValueError(f"意外的记号 col={t[2]}")\n\n    val = parse_expr()\n    expect_end()\n    return val\n\n\ndef _tokenize(s):\n    toks, i, n = [], 0, len(s)\n    while i < n:\n        ch = s[i]\n        if ch.isspace():\n            i += 1\n            continue\n        col = i + 1\n        if ch.isdigit() or ch == ".":\n            j = i\n            dots = 0\n            while j < n and (s[j].isdigit() or s[j] == "."):\n                if s[j] == ".":\n                    dots += 1\n                j += 1\n            text = s[i:j]\n            if dots > 1:\n                raise ValueError(f"非法数字 col={col}")\n            toks.append(("num", float(text) if "." in text else int(text), col))\n            i = j\n            continue\n        if s.startswith("**", i):\n            toks.append(("op", "**", col))\n            i += 2\n            continue\n        if ch in "+-*/":\n            toks.append(("op", ch, col))\n            i += 1\n            continue\n        if ch == "(":\n            toks.append(("lp", ch, col))\n            i += 1\n            continue\n        if ch == ")":\n            toks.append(("rp", ch, col))\n            i += 1\n            continue\n        raise ValueError(f"非法字符 {ch!r} col={col}")\n    return toks\n',
    'X6': '\nfrom collections import OrderedDict\n\n\nclass Cache:\n    def __init__(self, max_weight):\n        if max_weight <= 0:\n            raise ValueError("max_weight 必须为正")\n        self.max_weight = max_weight\n        self._d = OrderedDict()          # key -> [value, weight, expire_at or None]\n        self._w = 0\n        self.hits = 0\n        self.misses = 0\n        self.evictions = 0\n        self.expirations = 0\n\n    def _drop(self, key):\n        _, w, _ = self._d.pop(key)\n        self._w -= w\n\n    def _sweep_expired(self, now):\n        \"\"\"清掉所有已过期的项：算 expirations，不算 evictions。\"\"\"\n        for k in [k for k, (_, _, e) in self._d.items() if e is not None and now > e]:\n            self._drop(k)\n            self.expirations += 1\n\n    def peek(self, key, now=0.0):\n        \"\"\"只看不动：不刷新 LRU、不计数、不清理过期项。\"\"\"\n        item = self._d.get(key)\n        if item is None:\n            return None\n        value, _, exp = item\n        if exp is not None and now > exp:\n            return None\n        return value\n\n    def put(self, key, value, weight=1, ttl=None, now=0.0):\n        if weight > self.max_weight:     # 严格大于才拒绝；恰好等于要收下\n            return\n        if key in self._d:\n            self._drop(key)\n        exp = None if ttl is None else now + ttl\n        self._d[key] = [value, weight, exp]\n        self._w += weight\n        if self._w > self.max_weight:\n            self._sweep_expired(now)     # 先清过期，清完可能就不超了\n        while self._w > self.max_weight:\n            old = next(iter(self._d))\n            if old == key:               # 别把刚放进来的这条淘汰掉\n                break\n            self._drop(old)\n            self.evictions += 1\n\n    def get(self, key, now=0.0):\n        item = self._d.get(key)\n        if item is None:\n            self.misses += 1\n            return None\n        value, _, exp = item\n        if exp is not None and now > exp:\n            self._drop(key)              # 过期属于 expirations，不算 evictions\n            self.expirations += 1\n            self.misses += 1\n            return None\n        self._d.move_to_end(key)         # get 也算一次使用\n        self.hits += 1\n        return value\n',
}
