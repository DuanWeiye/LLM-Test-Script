#!/usr/bin/env python3
"""维度：深度诊断（开卷 / 闭卷对照）。

**换题的原因**：上一版用的是本机真实故障（SIM7080G 的 SH 栈撞锁），标准答案就躺在
`~/Documents/md/` 里。为了让闭卷题真的是闭卷，每次评测都得先
`sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0` 把 bwrap 放出来遮蔽那个目录，
跑完再改回去 —— 太不方便，忘了改回去还是长期降低本机安全性。

新题的答案是**为这套评测虚构的**（GW-7 网关的 TLS 会话槽位泄漏），本机任何地方都没有这份资料，
于是：**闭卷 = 项目里不放 docs/，开卷 = 放 docs/**，一个 `drop` 参数就够，不需要任何特权。
顺带还更可控 —— 干扰文档放几篇、答案藏多深，都能调。

故障设计（保持与原题同构的难度来源）：
  · 报错字面意思有误导性（`no buffer available` 听着像内存不够，其实是槽位表满）
  · 现象反直觉（内存充足、网络通、服务端正常，重启就好）
  · 有明确触发条件（夜间批量补传并发建连、结束时没做 TLS 正常关闭）
  · 立即恢复手段与根治手段不同，另有工程上的妥协做法

判分交给 LLM-judge（判结论语义，不判措辞），关键词只做对照快筛。
"""
from __future__ import annotations

import re

from core.judge import Rubric

from . import Case

RUBRIC = Rubric(
    points={
        "槽位耗尽非内存不足":
            "指出 `no buffer available` 是 TLS 会话槽位表被占满（8/8），而不是设备内存不够",
        "槽位只占不还":
            "指出被 retain 的槽位不会自动回收/没有超时 GC，所以故障不会自愈、必须干预",
        "触发=批量并发且未正常关闭":
            "指出触发条件是夜间批量补传并发建了多条连接、结束时没有做 TLS 正常关闭"
            "（close_notify），于是每条都漏一个槽位",
        "排除网络与服务端":
            "明确把网络/信号/带宽/DNS/服务端拒绝这一类解释排除掉，说明链路本身是通的",
        "立即恢复=重启TLS子系统":
            "给出让它马上恢复的手段：重启 TLS 子系统（或整机重启）以清空槽位表",
        "根治=复用连接并正常关闭":
            "给出根治改法：批量传输复用同一条连接、并且每条连接都要正常关闭（发 close_notify）",
        "工程妥协":
            "给出改不动上层时的折中：把并发数压到小于槽位总数，或批量任务结束后/定时重启 TLS 子系统",
    },
    negatives={
        "归因网络": "把信号弱、带宽不足、丢包、DNS 当成根本原因或解决方案",
        "归因内存": "把内存不足、内存泄漏、OOM 当成根本原因（被 `no buffer available` 字面误导）",
        "归因服务端": "把服务端限流、服务端拒绝、证书过期当成根本原因",
    },
)

PROMPT = ("看一下 incident.md 这个现场故障单。这台网关到底是怎么回事？"
          "我要能落地的东西：根因是什么、为什么重启就好、现在怎么让它立刻恢复、"
          "以及要改成什么样才不会再犯。")

# ---------- 裁判的考卷 ----------
# 裁判自己也要有 oracle。这些样本的标注是人工定的，裁判判不对就不许拿它的结论写结论。
# 覆盖四种历史上真实翻过车的情形：同义改写、只提关键词没结论、被误导性报错带偏，
# 以及最阴的一种 —— **提到网络/内存只为把它排除掉**（关键词判分必然在这里假阳性）。
_ANSWER_FULL = """
根因是 TLS 会话槽位泄漏，不是内存问题也不是网络问题。

`no buffer available` 这个报错的字面意思很有误导性：它说的不是设备内存不够，
而是 TLS 子系统那张固定 8 个格子的会话槽位表被占满了（日志里 in_use 一路涨到 8/8，
之后 handshake refused）。设备内存还剩 700MB，free 和 dmesg 都能佐证。

网络和服务端都可以排除：同一时刻 ping 通、curl healthz 返回 200、DNS 正常，
服务端访问日志里根本没有这些请求的记录，说明连接压根没建起来，问题出在本机 TLS 层。

触发条件是凌晨 02:00 那次批量补传：它并发起了 6 条连接，跑完之后直接收工，
没有做 TLS 层的正常关闭，日志里对应六条 "peer socket gone, no close_notify received
(slot retained)"。这套 TLS 实现只有收到 close_notify 才释放槽位，被 retain 的槽位
既不超时也没有 GC，所以漏掉的槽位永远不会还回来 —— 这就是为什么故障不会自愈，
而重启能好：重启把整张槽位表清零了。

立刻恢复：重启 TLS 子系统就行，不必重启整机（gw7ctl restart tls）。

根治：批量上报改成复用同一条 keep-alive 连接，别一批开一堆；每条连接用完必须走
正常关闭流程把 close_notify 发出去，不能直接关 socket 或者靠进程退出顺带关。

如果短期改不动上层程序，折中办法是把批量的并发数压到小于 8，
并且在批量任务结束后主动重启一次 TLS 子系统，把漏掉的槽位清掉。
"""

_ANSWER_PARAPHRASE = """
这台机器断的是加密握手，不是链路。

那句「缓冲区不足」很容易让人以为是 RAM 不够，其实指的是加密模块里那张会话表 ——
它一共就八个位置，日志显示占用数从 1 一路加到 8 就再没降下来过，之后所有握手一律被拒。
内存实际剩七百多兆，跟这事没关系。

链路侧我认为可以划掉：延迟三毫秒不丢包，同一台机器上 curl 健康检查是 200，
云端那边压根没收到请求，说明会话在本地就没建起来。

事情出在两点整那次补传：它一口气拉了六路并行连接，收尾时没有按加密协议的规矩
发结束通知就散了，于是那六个位置全被挂住不放。这套实现没有回收机制，
挂住的位置不会自己松开，所以它不可能自己好，而断电重来相当于把整张表抹掉，自然就通了。

要马上恢复，把加密子系统重新起一遍即可，整机重启也行但没必要。

要彻底解决，补传应当只用一条长连接把数据依次发完，并且每条连接结束时都走完整的
关闭流程，把结束通知发出去，而不是直接把 socket 关掉。

暂时改不了程序的话，就把并行数降到八以下，再让补传任务收尾时顺手把加密子系统重启一次。
"""

_ANSWER_MEM = """
从报错 "no buffer available" 看，这是典型的内存不足。批量补传时一次性加载了 4213 条积压数据，
内存峰值把可用 buffer 吃光了，导致后续 TLS 握手申请不到内存而失败。重启后内存回收，所以恢复正常。

建议：把批量补传改成分页读取，减小单次内存占用；给上报进程加内存上限告警；
必要时给设备加内存或者开启 swap。
"""

_ANSWER_NET = """
应该是夜间网络质量的问题。批量补传占满了上行带宽，导致后续的 TLS 握手包大量丢失，
握手超时失败。白天带宽空闲就没事，凌晨和客户的备份任务撞车就出问题。

建议：给上报做限速，避开客户备份窗口；检查一下无线信号强度，必要时加装外置天线；
也可以让运营商确认一下夜间是否有 QoS 策略。
"""

_ANSWER_VAGUE = """
日志里能看到 slot、in_use、close_notify 这些字样，upload.log 里有 tls handshake failed。
故障发生在凌晨批量补传之后，重启可以恢复。

建议进一步排查 TLS 相关配置，收集更多日志，必要时联系厂商确认固件版本的已知问题。
"""

# 最阴的一种：结论完全正确，但通篇在**枚举并排除**网络/内存假设。
# 关键词判分会因为出现「带宽」「内存」「信号」而误判成甩锅，裁判必须判成没甩锅。
_ANSWER_EXCLUDES = """
先把几个常见的怀疑对象排掉：不是带宽不够，也不是信号差 —— ping 3ms 不丢包，
curl 健康检查 200；不是内存泄漏或 OOM，free 还有 700MB、dmesg 干净；
也不是服务端限流或证书问题，服务端连请求都没收到，而且重启后同一张证书立刻就能用。

真正的原因在本机 TLS 子系统：会话槽位表只有 8 个格子，日志里 in_use 单调涨到 8/8 之后
所有握手被拒，报的 no buffer available 说的是槽位表满，不是内存。
02:00 的批量补传并发开了 6 条连接，收尾时没有发 close_notify，
六个槽位全被标成 retained 留住了；这套实现对 retained 槽位没有超时回收，
所以漏了就永远不还，故障也就不会自愈，而重启会把整张表清零。

立即恢复重启 TLS 子系统即可。要根治，批量传输应复用同一条连接、并且每条连接
都要正常关闭把 close_notify 发出去。短期方案是并发数压到 8 以下，
并在批量结束后主动重启一次 TLS 子系统。
"""

_ALL_TRUE = {k: True for k in RUBRIC.points}
_ALL_CLEAN = {k: False for k in RUBRIC.negatives}

JUDGE_FIXTURES = [
    {"name": "参考解（原文表述）", "text": _ANSWER_FULL,
     "points": _ALL_TRUE, "negatives": _ALL_CLEAN},
    {"name": "参考解（完全同义改写）", "text": _ANSWER_PARAPHRASE,
     "points": _ALL_TRUE, "negatives": _ALL_CLEAN},
    {"name": "错判成内存不足", "text": _ANSWER_MEM,
     "points": {"槽位耗尽非内存不足": False, "触发=批量并发且未正常关闭": False,
                "根治=复用连接并正常关闭": False, "排除网络与服务端": False},
     "negatives": {"归因内存": True, "归因网络": False}},
    {"name": "错判成网络/带宽", "text": _ANSWER_NET,
     "points": {"槽位耗尽非内存不足": False, "根治=复用连接并正常关闭": False},
     "negatives": {"归因网络": True, "归因内存": False}},
    {"name": "只复述现象没结论", "text": _ANSWER_VAGUE,
     "points": {"槽位耗尽非内存不足": False, "槽位只占不还": False,
                "触发=批量并发且未正常关闭": False, "根治=复用连接并正常关闭": False},
     "negatives": _ALL_CLEAN},
    {"name": "正确且主动排除干扰假设", "text": _ANSWER_EXCLUDES,
     "points": _ALL_TRUE, "negatives": _ALL_CLEAN},
]

# ---------- 关键词快筛（只做对照，不参与判分） ----------
_SCREEN = {
    "槽位耗尽非内存不足": r"槽位|slot|会话表|8/8",
    "槽位只占不还": r"不会(自动)?(回收|释放)|没有\s*GC|不超时|永远不还|retain",
    "触发=批量并发且未正常关闭": r"close_notify|正常关闭|优雅关闭|并发.*(连接|worker)",
    "排除网络与服务端": r"不是网络|非网络|排除.*(网络|带宽)|链路(正常|是通的)",
    "立即恢复=重启TLS子系统": r"重启.*(TLS|tls|加密)|restart\s+tls",
    "根治=复用连接并正常关闭": r"复用.*连接|keep-?alive|长连接",
    "工程妥协": r"并发.*(小于|少于|压到|降到)|定时重启|批量.*后.*重启",
}


def keyword_screen(text: str, rubric=None) -> dict:
    """关键词快筛。与裁判结论并列存进结果，两者分歧的样本值得人看一眼。

    它**不是**判分依据 —— 匹配的是措辞而非结论，历史上正是这一点造成过双向误判。
    """
    return {k: bool(re.search(pat, text, re.IGNORECASE)) for k, pat in _SCREEN.items()}


CASES = [
    Case(id="E1", dim="诊断·开卷", suite="gw7_field", prompt=PROMPT, k=3,
         rubric=RUBRIC, timeout=900,
         notes="项目里带 docs/（1 篇答案 + 3 篇干扰），测会不会主动翻资料"),
    Case(id="E1I", dim="诊断·闭卷", suite="gw7_field", prompt=PROMPT, k=3,
         rubric=RUBRIC, drop=["docs"], timeout=900,
         notes="同一现场，拿掉 docs/ —— 只能靠日志里的 in_use/close_notify 线索硬推"),
]
