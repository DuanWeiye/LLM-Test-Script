[project]
name = "telemetry_kit"
version = "0.1.0"
description = "设备遥测数据处理（解析 / 聚合 / 阈值告警）"
requires-python = ">=3.9"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["telemetry_kit*"]
