# ops_scripts

跑在推理机上的几个运维小脚本，都是 cron 或手动调起来的。

| 脚本 | 干什么 | 备注 |
|---|---|---|
| `healthcheck.py`   | 探活推理服务，挂了就写告警日志 | 每分钟一次 |
| `sync_backup.py`   | 把模型目录同步到备份盘 | 每天凌晨 |
| `watch_gpu.py`     | 采样 GPU 温度/占用，超阈值告警 | 常驻 |
| `fetch_metrics.py` | 抓上一轮采样结果汇总成表 | 手动 |

阈值之类的配置都在 `config/` 下，别写死在代码里。
